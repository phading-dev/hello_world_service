{
  "tables": [{
    "name": "User",
    "columns": [{
      "name": "userId",
      "addColumnDdl": "ALTER TABLE User ADD COLUMN userId STRING(MAX) NOT NULL"
    }, {
      "name": "data",
      "addColumnDdl": "ALTER TABLE User ADD COLUMN data BYTES(MAX) NOT NULL"
    }],
    "createTableDdl": "CREATE TABLE User (userId STRING(MAX) NOT NULL, data BYTES(MAX) NOT NULL) PRIMARY KEY (userId ASC)",
    "indexes": []
  }]
}