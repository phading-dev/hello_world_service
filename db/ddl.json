{
  "tables": [{
    "name": "User",
    "columns": [{
      "name": "userId",
      "addColumnDdl": "ALTER TABLE User ADD COLUMN userId STRING(MAX) NOT NULL"
    }, {
      "name": "createdTimeMs",
      "addColumnDdl": "ALTER TABLE User ADD COLUMN createdTimeMs FLOAT64 NOT NULL"
    }],
    "createTableDdl": "CREATE TABLE User (userId STRING(MAX) NOT NULL, createdTimeMs FLOAT64 NOT NULL) PRIMARY KEY (userId ASC)",
    "indexes": []
  }]
}